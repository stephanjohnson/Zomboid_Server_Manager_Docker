{
	# Managed by setup.sh — edit freely or re-run make init
}

:443 {
	# Use Caddy's internal CA by default so fresh environments work without external cert files.
	tls internal
	reverse_proxy app:8000
}

:80 {
	redir https://{host}{uri} permanent
}
